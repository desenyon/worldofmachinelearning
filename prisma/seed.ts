/**
 * Reference seed scaffold for future Prisma-backed storage.
 * Current app state uses data/program-state.json for local-first development.
 */

async function main() {
  console.log('Seed placeholders: lessons, badges, devices, demo learner snapshots.');
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
